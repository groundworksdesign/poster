# REQ-006: Drag-and-drop reorder

## Requirement

Reorder slides with drag-and-drop instead of up/down arrows. Keep Top and Move-to-number.

## Acceptance (from epic Validation)

- [ ] No up/down arrow reorder buttons on slide rows.
- [ ] Drag handle reorders slides (same `reorderSlide` behavior).
- [ ] **Top** and **Move** still work.
- [ ] **Showing** stays on the same slide (by id) after reorder.

## Current code pointers

- `src/Deck/DeckBuilder.tsx`: `moveSlide` (↑/↓), `reorderSlide`, `adjustPresentIndexAfterReorder`
- `src/Deck/DeckBuilder.unit.test.tsx`: Top/Move tests

## Test plan

- Unit test: simulate drag reorder; assert order and `presentSlideIndex` follows slide id.
