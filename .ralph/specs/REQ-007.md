# REQ-007: Export / Save toolbar and Home library refresh

## Requirement

Toolbar: current Save becomes **Export** (file). Current Save to Library becomes **Save**. Save updates the Home presentation list in place (no full page refresh).

## Acceptance (from epic Validation)

- [ ] Toolbar button **Export** downloads deck JSON file (today’s `handleSaveClick`).
- [ ] Toolbar button **Save** persists to library (today’s `handleSaveToLibrary`).
- [ ] After Save, Home library list updates in place without full page reload (separate window/tab case).

## Current code pointers

- `src/Deck/DeckBuilder.tsx`: `#save`, `#save-to-library`, `handleSaveClick`, `handleSaveToLibrary`, unused `libraryRefreshKey`
- `src/HomePage.tsx` + `src/Deck/LibraryPanel.tsx`: `refreshKey` only local to Home mount

## Test plan

- Unit test: button labels/ids; Save calls `/library/save`; cross-tab refresh mechanism updates `LibraryPanel` entries.
