# REQ-003: More workspace width

## Requirement

The deck edit area uses more of the page width.

## Acceptance (from epic Validation)

- [ ] Workspace uses more page width than current PR #12 layout (list was `42%` + full editor column).

## Current code pointers

- `src/App.css`: `.deck-slides-editor-row`, `.deck-slides-list`, `.deck-slide-editor`
- `src/Deck/DeckBuilder.tsx`: layout structure

## Test plan

- CSS/layout assertion or snapshot: slides list + rail occupy greater share of `.deck-page` max width; no whole-app restyle.
