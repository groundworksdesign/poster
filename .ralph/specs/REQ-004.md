# REQ-004: Collapsed Edit rail

## Requirement

The slide editor collapses to the side when nothing is being edited.

## Acceptance (from epic Validation)

- [ ] No slide being edited → collapsed **Edit** rail on the right (per mock).
- [ ] **Edit** on a row expands the editor.
- [ ] **Cancel** or **Save slide** collapses back to the rail.

## Current code pointers

- `src/Deck/DeckBuilder.tsx`: `selectedSlideIndex`, `.deck-slide-editor`, placeholder text
- `src/App.css`: editor column styles

## Test plan

- Unit test: default collapsed rail visible; Edit expands; Cancel/Save slide collapses.
