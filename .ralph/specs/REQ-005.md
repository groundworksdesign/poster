# REQ-005: Prominent Save slide

## Requirement

Saving a slide’s edits is more obvious (prominent Save in the slide editor).

## Acceptance (from epic Validation)

- [ ] Prominent **Save** control in the expanded slide editor (not Export, not library Save).
- [ ] Save persists that slide’s edits (fields/style/lyrics) to deck state.

## Current code pointers

- `src/Deck/DeckBuilder.tsx`: `updateSlideField`, `updateSlideStyle`, editor panel (edits are live today)

## Test plan

- Unit test: edit field, click Save slide, assert deck state and optional collapse (REQ-004); edits survive re-open Edit.
