# REQ-005 — Window-scoped only

## Statement

Only existing Present windows are fullscreened, on the display they already occupy (no display-picker UI).

## Acceptance

- [ ] IPC uses `BrowserWindow.fromWebContents(event.sender)` only
- [ ] No display-picker UI or multi-display move APIs
