# REQ-003 — F and Escape always exit

## Statement

`F` and Escape always restore windowed Present; no hard kiosk that blocks easy exit.

## Acceptance

- [ ] Escape calls the same exit path as leaving via `F`
- [ ] Exit clears native and fallback fullscreen
- [ ] No kiosk / `setKiosk(true)` lock
