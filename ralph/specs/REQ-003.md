# REQ-003: Config CI script paths

## Requirement

Runtime and packaging path references match the new layout; `remix` `appDirectory`, `package.json` main/scripts, electron-builder and portable scripts align with adapters.

## Acceptance (from epic Validation)

- [x] `remix.config.js` `appDirectory` is `src/adapters/remix`.
- [x] `package.json` `main` and start scripts match adapters.
- [x] electron-builder and portable packaging include/copy new adapter/domain paths.
- [x] Operator-facing README path guidance for start/e2e/electron matches the new layout.

## Current code pointers

- `remix.config.js`, `package.json`, `electron-builder.yml`
- `scripts/package-portable.mjs`, `scripts/package-portable.sh`, `scripts/prepare-electron-pack.cjs`
- `README.md` (operator paths); historical `docs/epics/*` are lower priority

## Test plan

- File existence checks for packaged entries; read scripts for stale `server/index.js` / root `electron/` assumptions that still affect packaging.

## Durable guard

- `src/__tests__/config-path-alignment.test.js` — asserts package.json main/start, remix appDirectory, e2e script configs, electron-builder/portable entries, and operator README paths.
