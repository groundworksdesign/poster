# REQ-004 — No auto-fullscreen on open

## Statement

Present does not enter fullscreen automatically when the window opens.

## Acceptance

- [ ] `openPresentSessionWindow` does not call `setFullScreen` / `setSimpleFullScreen`
- [ ] Fullscreen only after operator `F` (or explicit UI mapped to the same path)
