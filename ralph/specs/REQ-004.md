# REQ-004: Automated suites green

## Requirement

Unit, integration, and e2e that should pass do pass after move fixes; repair remaining move-induced failures.

## Acceptance (from epic Validation)

- [ ] `tsc --noEmit` and Jest unit/integration pass (or out-of-scope failures documented with evidence).
- [ ] `pnpm run test:e2e:remix` and `pnpm run test:e2e:electron` pass after path fixes (or documented pre-existing failures).

## Current code pointers

- Unit/integration under `src/**` and `src/__tests__`, `src/integration`
- Playwright under `tests/e2e`
- CI: `.github/workflows/pr.yml` jobs `unit-integration`, `remix-e2e`, `electron-smoke`

## Test plan

- Run the same commands CI uses; fix only move-induced breakages unless a tiny adjacent fix is required to keep the suite honest.
