# REQ-004: Automated suites green

## Requirement

Unit, integration, and e2e that should pass do pass after move fixes; repair remaining move-induced failures.

## Acceptance (from epic Validation)

- [x] `tsc --noEmit` and Jest unit/integration pass (or out-of-scope failures documented with evidence).
- [ ] `pnpm run test:e2e:remix` and `pnpm run test:e2e:electron` pass after path fixes (or documented pre-existing failures).

## Current code pointers

- Unit/integration under `src/**` and `src/__tests__`, `src/integration`
- Playwright under `tests/e2e`
- CI: `.github/workflows/pr.yml` jobs `unit-integration`, `remix-e2e`, `electron-smoke`

## Test plan

- Run the same commands CI uses; fix only move-induced breakages unless a tiny adjacent fix is required to keep the suite honest.

## Dev evidence (awaiting QA)

- `pnpm exec tsc --noEmit` — exit 0
- `CI=true pnpm exec react-scripts test --watchAll=false --runInBand` — 41 suites / 254 tests passed
- No code fixes required on this tip (already green after prior harden work)

## QA evidence

- Tip `2b563c2` independently re-ran CI unit-integration commands (2026-09-14T15:52:35.000Z).
- tsc --noEmit exit 0; Jest 41 suites / 254 tests passed.

## Dev evidence (e2e; awaiting QA)

- repoRoot.selfcheck -> workspace root (helpers-under-tests gone)
- pnpm run test:e2e:remix -> 52 passed
- pnpm run test:e2e:electron -> 9 passed (production Remix build)
- Hardening: tests/e2e/electron.global-setup.cjs rebuilds production build when build/ still has jsx-dev-runtime (remix e2e pollution)
