# REQ-010: AppImage Present client hydrate with real presentId URL + preload

## Requirement

On packaged Linux AppImage, cold Open Present must reliably leave SSR `Loading...` and reach client `present-ready` **even when the window already has preload (`window.poster`) and a real `presentId` Present URL** (main-owned `present-session` `loadURL` — **not** `about:blank`).

Jack tip `c3a1642` showed REQ-009’s about:blank / named-window harden is **insufficient**: blank-deck + import cold Present stayed flaky **7/12 (~58%)** with preload + real URL still stuck on Loading.

## Acceptance (from epic Validation)

- [ ] AppImage blank-deck (incl. after file import): cold Open Present reaches `present-ready` reliably across cold repeats when URL is already `/presentation?...&presentId=...` with preload (not flaky ≥~1/2 fail).
- [ ] Library Open → Present remains reliable (no REQ-008 regression; watch tip `c3a1642` later 1/2 flake).
- [ ] Tip Electron / automated Electron e2e remain green; do not treat tip-Electron PASS alone as AppImage proof.

## Jack baseline (tip `c3a1642`) — NOT READY

| Area | Result |
| --- | --- |
| **Blank-deck + import cold Present (AppImage)** | **FAIL (flaky) 7/12 (~58%)** — SSR Loading with **preload + real presentId URL** (not about:blank) |
| Library Open → Present | Primary **3/3 PASS**; later **1/2 flake** |
| Tip unpackaged Electron (blank + library) | PASS |

## Distinct from prior REQs

| REQ | Failure mode |
| --- | --- |
| REQ-006 / #18 | First-open / about:blank / main-owned session (blank-deck) |
| REQ-008 | Library Open path / noopener / window policy |
| REQ-009 | Import / named window / late `window.poster` → about:blank fallthrough |
| **REQ-010** | **Client hydrate race when session URL + preload are already correct** |

## Hypothesis (for DEV)

Under AppImage/FUSE: main-owned `present-session` `loadURL` + Remix SSR first paint + `present-ready` handshake timing — preload may not finish before first paint; Start/send may still race; tip Electron rarely hits this.

## Current code pointers

- `src/adapters/electron/main.cjs` — main-owned present-session `loadURL`
- `src/adapters/electron/homeWindowPolicy.cjs` — `present-session` classification
- Present route client boot / `present-ready` testid
- Deck Start gate / session graph replay (REQ-009) — may still race hydrate

## Test plan

- Prefer AppImage cold repeats: Open Present (blank-deck ± import) ×N; on FAIL confirm URL has `presentId` and preload present while stuck Loading.
- Automated: `presentHydrateWatchdog` unit + main regression; `electron-present-hydrate-stress.spec.ts` (cold Open Present ×3 with presentId URL + preload + client boot).
- Keep Library Open → Present green.
