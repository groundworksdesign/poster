# REQ-008: Library Open → Present first-open hydrate (packaged)

## Requirement

After opening a saved library entry into Deck on packaged Electron/AppImage, the first Open Present must leave SSR `Loading...` and reach client `present-ready` within a reasonable timeout (not stuck ≥30s). This path is **distinct** from blank-deck cold Open Present (REQ-006 / main-owned `present-session`); Jack tip `497d454` shows blank-deck Open Present + send 3/3 PASS while Library Open → Present FAIL.

## Acceptance (from epic Validation)

- [x] Library Open → Deck → Open Present on packaged Electron/AppImage reaches `present-ready` (not stuck SSR Loading ≥30s).
- [x] Blank-deck Open Present path remains reliable (no regression of REQ-006).
- [x] Investigation confirms whether Library Deck open (`noopener` / named window / `location.assign` fallback) drops `window.poster` or routes Present via `about:blank` / `present-blank` instead of main-owned `present-session`.

## Jack baseline (tip `497d454`) — NOT READY

| Area | Result |
| --- | --- |
| Blank-deck cold Open Present + send | **3/3 PASS** |
| **Library Open → Present** | **FAIL** — SSR Loading ≥30s (does not hydrate) |
| Message-only, On Program, theme, library root, Home | PASS |

## Current code pointers

- `src/presentation/HomePage.tsx` — `handleOpenFromLibrary` (`noopener,noreferrer`, named `posterDeck`, assign fallback) vs blank `openDeckWindow` (`_blank`, no noopener)
- `src/presentation/Deck/DeckBuilder.tsx` — `?open=` library load; `handleOpenPresent`
- `src/presentation/Deck/openPresentWindow.ts` — `planPresentOpen` / `hasPosterBridge` → `electron-direct` vs `browser-gesture-blank` (`about:blank`)
- `src/adapters/electron/main.cjs` — `openPresentSessionWindow` + `setWindowOpenHandler` deny for `present-session`
- `src/adapters/electron/homeWindowPolicy.cjs` — `present-session` vs `present-blank` classification
- Electron e2e today covers blank Open Presentation → Present, **not** Library Open → Present

## Hypothesis (for DEV)

After Library Open, Present may still hit popup/`about:blank` (`present-blank` allow) or wrong URL/session vs blank-deck Open Present — especially if the Library-opened Deck shell lacks preload / `window.poster` so `planPresentOpen` falls back to `browser-gesture-blank`.

## Test plan

- Packaged AppImage / Electron: Library Open → Open Present must hydrate; compare with blank-deck Open Present.
- Prefer adding Electron e2e: library-open → deck loaded → Present `present-ready`.
- Do not treat blank-deck-only PASS as covering this REQ.

## QA evidence

- Tip `a4a1a18` independently reviewed (2026-09-14T23:52:47.000Z).
- Library Open uses `_blank` + `POPUP_FEATURES` (no noopener); matches blank-deck Open Presentation.
- Unit: HomePage REQ-008 + blank-deck Open Presentation PASS.
- Electron: `electron-library-present-hydrate.spec.ts` PASS; `electron-program-thumbnail.spec.ts` (blank-deck) PASS.
- openPresentWindow / DeckBuilder Electron Open Present unit PASS.
- Packaged AppImage binary not re-run here — Jack retest remains for final READY.
- Draft PR #21 stays draft.
