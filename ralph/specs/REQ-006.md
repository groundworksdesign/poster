# REQ-006: Present first-open hydrate (packaged)

## Requirement

Cold Open Present on packaged Electron/AppImage reliably leaves SSR `Loading...` and reaches client `present-ready`. Restore PR #18 first-open behavior if it regressed under the clean-architecture layout move.

## Acceptance (from epic Validation)

- [ ] Packaged/AppImage cold Present first-open is reliable (not flaky stuck on Loading).
- [ ] Client hydrate reaches `present-ready` / clears `present-loading`.
- [ ] Investigate layout-move interaction with PR #18 (about:blank vs direct `/presentation?...`, preload/session bootstrap under `src/adapters/electron`).

## Jack baseline (tip `9f124e1`)

- NOT READY: 1/3 PASS, 2/3 stuck SSR Loading on packaged AppImage.

## Current code pointers

- `src/presentation/Present/Present.tsx` — `Loading...` / `present-ready` / `createPresentSession`
- `src/adapters/electron/main.cjs` — Present window open / about:blank notes
- `src/adapters/electron/homeWindowPolicy.cjs` — present-session classification; comments on AppImage cold open hydrate
- `tests/e2e/electronHelpers.ts` — waits for `present-ready` (not URL-only)

## Test plan

- Prefer packaged/AppImage or Electron e2e that proves first-open hydrate; do not rely on URL navigation alone.
- Document residual flakiness if any remain for Jack’s checklist (REQ-005).
