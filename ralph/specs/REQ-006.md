# REQ-006 — Browser Present keeps HTML fullscreen

## Statement

Browser / Remix Present continues to use HTML `requestFullscreen` / `exitFullscreen` only.

## Acceptance

- [ ] Without `window.poster`, Present still calls `requestFullscreen`
- [ ] Electron path does not dual-enter HTML + native
