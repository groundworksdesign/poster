# REQ-009: AppImage blank-deck cold Present+send after file import

## Requirement

On packaged Linux AppImage, blank-deck cold Open Present + directed send after **file import** into Deck must reliably leave SSR `Loading...`, reach client `present-ready`, and complete send (not flaky ~1/3–1/2 fail). Tip unpackaged Electron and CI installer jobs already pass; this REQ targets the **packaged AppImage** path Jack exercises.

## Acceptance (from epic Validation)

- [ ] AppImage blank-deck: import file → Open Present → send reaches `present-ready` and send succeeds reliably across cold repeats (not flaky ≥1/3 fail).
- [ ] Library Open → Present remains reliable (no regression of REQ-008; Jack tip `7c947cd` 3/3 PASS).
- [ ] Tip Electron / automated Electron e2e remain green; do not treat tip-Electron PASS alone as AppImage proof.

## DEV harden (in-tree; awaiting Jack AppImage)

- Home Import CTA: `_blank` + `POPUP_FEATURES` (same as Open Presentation; not named `posterDeck`).
- `waitForPosterBridge` before `planPresentOpen` (AppImage/FUSE late preload after FileReader).
- Start gated on Present `child-ready` (`present-session-ready` / `presentation-start`).
- Electron e2e: `tests/e2e/electron-import-present-send.spec.ts` (import → Present → send).
- Residual risk note: `docs/appimage-residual-risk.md` tip `7c947cd` + Import→Present→send checklist.

## Jack baseline (tip `7c947cd`) — NOT READY

| Area | Result |
| --- | --- |
| **Library Open → Present** | **3/3 PASS** (REQ-008) |
| **Blank-deck cold Present + send after file import (AppImage)** | **FAIL (flaky)** — ~1/3–1/2 fail |
| Tip unpackaged Electron | PASS |
| CI installers | green |

## Current code pointers

- Home Import CTA → Deck with `focusImport` / file picker / `FileReader` load (async post-import)
- Deck Open Present → `openPresentForRuntime` / main-owned `present-session` (`loadURL`)
- Named Deck window (`posterDeck`) vs blank-deck `_blank` Open Presentation entry
- AppImage FUSE mount + cold first-open hydrate history (PR #18 / REQ-006)

## Hypothesis (for DEV)

AppImage/FUSE I/O or post-import timing/session race that tip Electron does not hit — e.g. Open Present/send before deck session/`window.poster`/Present child-ready settles after FileReader import, or named Deck window reuse/policy attach differences vs Library `_blank` path.

## Test plan

- Prefer AppImage cold repeats: Import file → Open Present → send ×N; record flake rate.
- Extend automated coverage where feasible (Electron e2e import → Present → send); still require Jack AppImage confirmation.
- Keep Library Open → Present green (REQ-008).
